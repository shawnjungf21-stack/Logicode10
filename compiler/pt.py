import os
print("""
This file called pt.py holds syntax data, and if deleted will break the parser.
""")
def syntaxes():
    if print():
        print() 
"""
Define Syntaxes for the Parser:
"""
'''

'''
{
    syntax.if: else
    syntax.else:
    syntax.if:
    syntax.block
    from Parser: .and
    boolvars = {
        display("")
        codeget
        logigraphics
        as
        if
        else
        elif
        if else
        then
        while
        with
        for
        to
        in
        properties""
        string
        script/ /
        html/ /
        and
        input
        output
        graph
        LCQT
        Qt
        cpp
        during
        __build_class__
        __init__
        //
        #define
        #local
        local.[var] or [module]
    }
}
on module lcqt