import sys
import logigraphics as gui
import tkinter
import pip
import showtext
import py
import syntax.Colors as Colors
import e
from e import e64
from e import e32
from tkinter import messagebox
from logigraphics import button, loadingbar, menu, frame, window, msgbox
display = {print("") }
CURRENT_SYNTAX_FILE = "CODEGET"
CODEGET_USAGE = "codeget (module)"
class modules:
    def module(logigraphics):
        with gui:
            def syntax(using, module_name, newname):
                if syntax(using, module_name, newname):
                    def gui(submodules):
                        submodules = "smod"
                        print(submodules)
                    def gui():
                        print("This syntax will be to create a submodules syntax array")
                        messagebox.error("Submodule Array is not Set Up.")
    def module(showtext):
        #the module showtext also has its own syntax arrays. We will work on this later.
        print("")
        #The empty print statement is to define how to use showtext.
        #Use display("") to define the text encoding if you want to not waste time.
        SYNTAX_DISP = display("")
        SYNTAX_DISP
        with SYNTAX_DISP:
            print()
    def module(py):
        py.module.__class__
        py.module.__get__
        py.module.__closure__
        py.module.__builtins__
        py.__module__: str
        py.__qualname__: str
        pip.main.__class__
        py.module.print("This module is kinda broken right now so the import properties won't work")
    def module(evga):
        with Colors:
            evga.__dir__
            evga.Colors
            evga.colordata()
            evga.module.__code__
            evga.messagebox.ERROR("Error")
        messagebox.ERROR("oops")
class modules():
    sys.modules.get.__builtins__
    sys.audit.__code__
    Colors
    string = "Hello World!"
    __module__: "string"